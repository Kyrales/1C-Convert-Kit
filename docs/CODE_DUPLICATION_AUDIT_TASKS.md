# Задачи по устранению дублирования кода

**Основа:** [SRC_CODE_DUPLICATION_AUDIT.md](file:///f:/1C/Projects/1c-convert-kit/docs/SRC_CODE_DUPLICATION_AUDIT.md)

**Дата создания:** 2026-02-03

**Статус:** Не начато

---

## Легенда

- **Приоритет:** 🔴 Высокий | 🟡 Средний | 🟢 Низкий
- **Сложность:** S (малая, 1-2 часа) | M (средняя, 3-6 часов) | L (большая, 1-2 дня)
- **Статус:** ⬜ Не начато | 🔄 В работе | ✅ Завершено

---

## Модуль: `src/converters/`

### Этап 1: Высокая критичность

#### 🔴 Задача 1.1: Рефакторинг подготовки базовой ИБ

**Приоритет:** 🔴 Высокий  
**Сложность:** L (1-2 дня)  
**Статус:** ✅ Завершено

**Описание:**  
Вынести дублированную логику подготовки базовой ИБ из `DataProcessorConverter` и `ExtensionConverter` в общий метод.

**Затронутые файлы:**
- `src/converters/dataprocessor/converter.py` (строки 108-223) ✅
- `src/converters/extension/converter.py` (строки 155-274) ✅
- `src/converters/base/tools.py` (добавлен helper `prepare_base_infobase`) ✅

**Подзадачи:**

- [x] 1.1.1 Проанализировать различия между двумя реализациями `_prepare_base_ib`
- [x] 1.1.2 Выбрать место для общей логики (helper в `src/converters/base/tools.py`)
- [x] 1.1.3 Рефакторить `DataProcessorConverter._prepare_base_ib` для использования базового метода
- [x] 1.1.4 Рефакторить `ExtensionConverter._prepare_base_ib` для использования базового метода
- [x] 1.1.5 Обновить существующие тесты
- [x] 1.1.6 Добавить unit-тесты для нового метода в `BaseConverter`

**Реализованное решение:**
```python
# В src/converters/base/tools.py добавлен helper:
def prepare_base_infobase(
    *,
    base_ib: Optional[str],
    base_config: Optional[str],
    temp_dir: Path,
    v8_tool: V8ToolWrapper,
    logger: Logger,
    entity_type: str = "обработок/отчетов"
) -> str:
    """
    Подготавливает базовую ИБ для загрузки обработок/отчетов/расширений.
    
    Логика:
    1. Если указан base_ib - использовать существующую ИБ
    2. Если указан base_config - создать ИБ и загрузить конфигурацию
    3. Иначе - создать пустую ИБ
    """
    # Реализация с явными зависимостями
```

**Тесты:**
- ✅ `tests/unit/test_dataprocessor_converter.py` - все 10 тестов прошли
- ✅ `tests/unit/test_extension_converter.py` - все 8 тестов прошли
- ✅ Полный набор unit-тестов - все 92 теста прошли

**Результат:**
- Устранено ~230 строк дублированного кода
- Создан переиспользуемый helper с явными зависимостями
- Все существующие тесты проходят успешно
- Код стал более поддерживаемым и тестируемым

---

#### 🔴 Задача 1.2: Рефакторинг импорта XML → EDT

**Приоритет:** 🔴 Высокий  
**Сложность:** L (1-2 дня)  
**Статус:** ⬜ Не начато

**Описание:**  
Выделить общий метод импорта XML в EDT проект из `ConfigurationConverter` и `ExtensionConverter`.

**Затронутые файлы:**
- `src/converters/configuration/converter.py` (строки 659-744)
- `src/converters/extension/converter.py` (строки 765-855)
- `src/converters/base/tools.py` (добавление метода в `EdtToolWrapper`)

**Подзадачи:**

- [ ] 1.2.1 Проанализировать различия между двумя реализациями импорта
- [ ] 1.2.2 Создать метод `import_configuration_files_to_edt_project` в `EdtToolWrapper`
- [ ] 1.2.3 Рефакторить блок импорта в `ConfigurationConverter`
- [ ] 1.2.4 Рефакторить блок импорта в `ExtensionConverter`
- [ ] 1.2.5 Обновить существующие тесты
- [ ] 1.2.6 Добавить unit-тесты для нового метода в `EdtToolWrapper`

**Предлагаемое решение:**
```python
# В EdtToolWrapper добавить:
def import_configuration_files_to_edt_project(
    self, 
    xml_source_path: Path,
    edt_project_path: Path,
    workspace_path: Path,
    *,
    edt_version: str
) -> int:
    """
    Импорт XML файлов в EDT проект.
    
    Args:
        xml_source_path: Путь к XML файлам
        edt_project_path: Путь к EDT проекту
        workspace_path: Путь к workspace
        entity_type: Тип сущности (configuration/extension)
        logger: Логгер для вывода
    
    Returns:
        Код возврата (0 - успех)
    """
    # Общая логика импорта через ring/edtcli:
    # - если edtcli: subprocess.run([...], shell=False)
    # - если ring: subprocess.run([...], shell=True) + передача edt_version
    # - унифицированная обработка returncode/кодировок
    # - проверка наличия маркера проекта (.project) после импорта
```

**Тесты для обновления:**
- `tests/integration/test_convert_integration.py` - обновить тесты конвертации conf2edt
- `tests/integration/test_convert_integration.py` - обновить тесты конвертации ext2edt

**Тесты для добавления:**
- Расширить `tests/integration/test_convert_integration.py`:
  - Тест успешного импорта конфигурации
  - Тест успешного импорта расширения
  - Тест обработки ошибок ring/edtcli
  - Тест обработки некорректных путей
  - Тест логирования процесса импорта

**Риски:**
- Критический путь конвертации - требуется особая осторожность
- Необходимо полное интеграционное тестирование

---

### Этап 2: Средняя критичность

#### 🟡 Задача 2.1: Рефакторинг цикла конвертации файлов обработок/отчетов

**Приоритет:** 🟡 Средний  
**Сложность:** M (3-6 часов)  
**Статус:** ⬜ Не начато

**Описание:**  
Вынести дублированный цикл загрузки файлов обработок/отчетов в приватный метод внутри `DataProcessorConverter`.

**Затронутые файлы:**
- `src/converters/dataprocessor/converter.py` (строки 311-366, 394-449)

**Подзадачи:**

- [ ] 2.1.1 Проанализировать различия между EDT и XML ветками
- [ ] 2.1.2 Создать приватный метод `_process_files_batch` в `DataProcessorConverter`
- [ ] 2.1.3 Рефакторить EDT ветку для использования нового метода
- [ ] 2.1.4 Рефакторить XML ветку для использования нового метода
- [ ] 2.1.5 Обновить существующие тесты
- [ ] 2.1.6 Добавить unit-тесты для нового метода

**Предлагаемое решение:**
```python
# В DataProcessorConverter добавить:
def _process_files_batch(
    self,
    processor_files: list,
    xml_path: Path,
    progress_base: int,
    progress_span: int,
    stage_description: str
) -> int:
    """
    Обработка пакета файлов обработок/отчетов.
    
    Args:
        processor_files: Список файлов для обработки
        xml_path: Путь к XML файлам
        progress_base: Базовое значение прогресса
        progress_span: Диапазон прогресса
        stage_description: Описание этапа для логирования
    
    Returns:
        Код возврата (0 - успех)
    """
    # Общая логика обработки файлов
```

**Тесты для обновления:**
- `tests/unit/test_dataprocessor_converter.py` - обновить тесты конвертации EDT→EPF
- `tests/unit/test_dataprocessor_converter.py` - обновить тесты конвертации XML→EPF

**Тесты для добавления:**
- Расширить `tests/unit/test_dataprocessor_converter.py` - добавить тесты для `_process_files_batch`:
  - Тест обработки одного файла
  - Тест обработки множества файлов
  - Тест обработки с ошибкой в середине пакета
  - Тест корректности расчета прогресса
  - Тест логирования этапов

**Риски:**
- Низкий риск, так как дублирование внутри одного файла

---

#### 🟡 Задача 2.2: Рефакторинг добавления учетных данных в V8ToolWrapper

**Приоритет:** 🟡 Средний  
**Сложность:** S (1-2 часа)  
**Статус:** ⬜ Не начато

**Описание:**  
Выделить helper-метод для добавления учетных данных к командам 1С в `V8ToolWrapper`.

**Затронутые файлы:**
- `src/converters/base/tools.py` (строки 340-347, 430-437)

**Подзадачи:**

- [ ] 2.2.1 Создать метод `_append_ib_credentials` в `V8ToolWrapper`
- [ ] 2.2.2 Рефакторить метод `DumpCfg` для использования helper
- [ ] 2.2.3 Рефакторить метод `LoadExternalDataProcessorOrReportFromFiles` для использования helper
- [ ] 2.2.4 Проверить другие методы на наличие аналогичного кода
- [ ] 2.2.5 Обновить существующие тесты
- [ ] 2.2.6 Добавить unit-тесты для нового метода

**Предлагаемое решение:**
```python
# В V8ToolWrapper добавить:
def _append_ib_credentials(self, cmd: list[str]) -> None:
    """
    Добавление учетных данных к команде 1С.
    
    Args:
        cmd: Список аргументов команды (модифицируется in-place)
    """
    ib_user = self.env_vars.get('V8_IB_USER', '')
    ib_pwd = self.env_vars.get('V8_IB_PWD', '')
    if ib_user:
        cmd.append(f'/N{ib_user}')
    if ib_pwd:
        cmd.append(f'/P{ib_pwd}')
```

**Тесты для обновления:**
- `tests/unit/test_registry_complete.py` - обновить тесты, использующие `DumpCfg`
- `tests/unit/test_dataprocessor_converter.py` - обновить тесты, использующие `LoadExternalDataProcessorOrReportFromFiles`

**Тесты для добавления:**
- Расширить существующие тесты конвертеров или добавить в `tests/unit/test_registry_complete.py`:
  - Тест добавления только имени пользователя
  - Тест добавления имени и пароля
  - Тест без учетных данных
  - Тест с пустыми учетными данными
  - Тест корректности формата команды

**Риски:**
- Минимальный риск, простой рефакторинг

---

## Модуль: `src/core/`

### Этап 3: Высокая критичность

#### 🔴 Задача 3.1: Унификация инфраструктуры вывода и логирования

**Приоритет:** 🔴 Высокий  
**Сложность:** L (1-2 дня)  
**Статус:** ⬜ Не начато

**Описание:**  
Создать единый модуль для вывода и логирования, устранив дублирование между `core/convert.py` и `converters/base/converter.py`.

**Затронутые файлы:**
- `src/core/convert.py` (строки 19-37, 39-57)
- `src/converters/base/converter.py` (строки 52-74, 76-129)
- Опционально: `src/utils/` (если решите выносить логирование в общий модуль)

**Подзадачи:**

- [ ] 3.1.1 Выбрать подход (рекомендуется минимальный без новых файлов)
- [ ] 3.1.2 Минимальный вариант: использовать `Logger/Colors` из `src/converters/base/converter.py` в `src/core/convert.py`
- [ ] 3.1.3 Удалить дублирующий `Colors` и `print_*` из `src/core/convert.py` (или сделать тонкими обертками над Logger)
- [ ] 3.1.4 Обновить тесты, которые импортируют/проверяют вывод (точечно)

**Предлагаемое решение:**
```python
# Рекомендуемый минимальный вариант:
# - core/convert.py использует Logger из converters/base/converter.py
# - core/convert.py не содержит собственной реализации Colors/print_*
```

**Тесты для обновления:**
- `tests/unit/test_params_descriptions.py` - обновить импорты
- `tests/unit/test_registry_complete.py` - обновить импорты
- Все тесты конвертеров - обновить импорты Logger

**Тесты для добавления:**
- Расширить существующие тесты конвертеров (`test_dataprocessor_converter.py`, `test_extension_converter.py`):
  - Тест класса Colors (все цветовые коды)
  - Тест Logger.info с различными кодировками
  - Тест Logger.success с различными кодировками
  - Тест Logger.error с различными кодировками
  - Тест Logger.warning с различными кодировками
  - Тест обработки ANSI escape кодов
  - Тест обработки BOM

**Риски:**
- Высокий риск - затрагивает весь вывод приложения
- Необходимо тщательное тестирование на Windows с различными кодировками

---

### Этап 4: Средняя критичность

#### 🟡 Задача 4.1: Унификация парсинга .env файлов

**Приоритет:** 🟡 Средний  
**Сложность:** M (3-6 часов)  
**Статус:** ⬜ Не начато

**Описание:**  
Использовать единую функцию `load_env_file` из `core/convert.py` в GUI модуле вместо дублированного парсинга.

**Затронутые файлы:**
- `src/core/convert.py` (строки 59-89)
- `src/gui/project_scanner.py` (строки 82-109)

**Подзадачи:**

- [ ] 4.1.1 Рефакторить `ProjectScanner._read_env_data` для использования `load_env_file`
- [ ] 4.1.2 Удалить дублированный код парсинга из `ProjectScanner`
- [ ] 4.1.3 Доработать `load_env_file`: поддержка BOM (`\ufeff`) и fallback-кодировок при чтении `.env`
- [ ] 4.1.4 Обновить существующие тесты GUI
- [ ] 4.1.5 Добавить тесты для `.env` с BOM/разными кодировками

**Предлагаемое решение:**
```python
# В ProjectScanner._read_env_data:
from ..core.convert import load_env_file

def _read_env_data(self, env_path: Path) -> tuple:
    """Чтение данных из .env файла."""
    try:
        env_data = load_env_file(env_path, silent=True)
        script_name = env_data.get('ScriptName', '')
        dst_path = env_data.get('V8_DST_PATH', '')
        return script_name, dst_path
    except Exception:
        return '', ''
```

**Тесты для обновления:**
- `tests/unit/test_project_editor_unit.py` - обновить тесты чтения .env
- `tests/unit/test_project_editor_auto.py` - обновить тесты сканирования проектов

**Тесты для добавления:**
- Расширить `tests/unit/test_project_editor_auto.py` или `tests/integration/test_convert_integration.py`:
  - Тест чтения корректного .env файла
  - Тест чтения .env с BOM
  - Тест чтения .env с различными кодировками (CP1251, UTF-8)
  - Тест обработки некорректного .env файла
  - Тест обработки отсутствующего файла

**Риски:**
- Низкий риск - GUI читает только 2 поля

---

## Модуль: `src/gui/`

### Этап 5: Средняя критичность

#### 🟡 Задача 5.1: Рефакторинг логики создания/копирования проектов

**Приоритет:** 🟡 Средний  
**Сложность:** M (3-6 часов)  
**Статус:** ⬜ Не начато

**Описание:**  
Вынести общую логику создания и копирования проектов в отдельный метод в `CyberpunkGUI`.

**Затронутые файлы:**
- `src/gui/main_window.py` (строки 694-742, 809-865)

**Подзадачи:**

- [ ] 5.1.1 Проанализировать различия между `_add_project` и `_copy_project`
- [ ] 5.1.2 Создать метод `_create_project_pipeline` в `CyberpunkGUI`
- [ ] 5.1.3 Рефакторить `_add_project` для использования pipeline
- [ ] 5.1.4 Рефакторить `_copy_project` для использования pipeline
- [ ] 5.1.5 Обновить существующие GUI тесты
- [ ] 5.1.6 Добавить unit-тесты для нового метода

**Предлагаемое решение:**
```python
# В CyberpunkGUI добавить:
def _create_project_pipeline(
    self,
    project_name: str,
    env_data: dict,
    success_message: str
) -> bool:
    """
    Общий pipeline создания проекта.
    
    Args:
        project_name: Имя проекта
        env_data: Данные для .env файла
        success_message: Сообщение об успехе
    
    Returns:
        True если успешно
    """
    # 1. Создать папку проекта
    # 2. Записать .env файл
    # 3. Пересканировать проекты
    # 4. Выбрать новый проект в таблице
    # 5. Обновить детали
    # 6. Показать сообщение об успехе
```

**Тесты для обновления:**
- Текущие `test_project_editor_*` тестируют диалог, а не пайплайн в `main_window.py`. При рефакторинге пайплайна лучше:
  - либо добавить отдельный unit-тест на “файловую часть” пайплайна (через `tempfile`, без GUI),
  - либо оставить как ручную проверку (так как GUI и потоки усложняют автотест).

**Тесты для добавления:**
- Расширить `tests/unit/test_project_editor_auto.py`:
  - Тест успешного создания проекта
  - Тест создания проекта с существующим именем
  - Тест создания проекта с некорректными данными
  - Тест корректности содержимого созданного `.env`
  - Тест обработки ошибок при создании папки

**Риски:**
- Средний риск - затрагивает UI логику

---

### Этап 6: Низкая критичность

#### 🟢 Задача 6.1: Централизация импорта GUI библиотеки

**Приоритет:** 🟢 Низкий  
**Сложность:** S (1-2 часа)  
**Статус:** ⬜ Не начато

**Описание:**  
Создать единый модуль для импорта FreeSimpleGUI/PySimpleGUI и использовать его во всех GUI модулях.

**Затронутые файлы:**
- `src/gui/main.py` (строки 9-14)
- `src/gui/main_window.py` (строки 17-22)
- `src/gui/project_editor.py` (строки 13-18)
- Опционально: `src/gui/sg_import.py` (новый файл)

**Подзадачи:**

- [ ] 6.1.1 Создать модуль `src/gui/sg_import.py`
- [ ] 6.1.2 Перенести логику импорта в новый модуль
- [ ] 6.1.3 Рефакторить `main.py` для использования нового импорта
- [ ] 6.1.4 Рефакторить `main_window.py` для использования нового импорта
- [ ] 6.1.5 Рефакторить `project_editor.py` для использования нового импорта
- [ ] 6.1.6 Обновить существующие GUI тесты
- [ ] 6.1.7 Добавить тесты для модуля импорта

**Предлагаемое решение:**
```python
# Создать src/gui/sg_import.py:
"""Централизованный импорт GUI библиотеки."""

try:
    import FreeSimpleGUI as sg
except ImportError:
    try:
        import PySimpleGUI as sg
    except ImportError:
        raise ImportError(
            "Требуется FreeSimpleGUI или PySimpleGUI. "
            "Установите: pip install FreeSimpleGUI"
        )

__all__ = ['sg']
```

**Тесты для обновления:**
- Все GUI тесты - обновить импорты

**Тесты для добавления:**
- Расширить существующие GUI тесты (`test_project_editor_auto.py` или `gui_test_helper.py`):
  - Тест успешного импорта FreeSimpleGUI
  - Тест fallback на PySimpleGUI
  - Тест ошибки при отсутствии обеих библиотек
  - Тест доступности объекта sg

**Риски:**
- Минимальный риск

---

#### 🟢 Задача 6.2: Унификация форматирования длительности

**Приоритет:** 🟢 Низкий  
**Сложность:** S (1-2 часа)  
**Статус:** ⬜ Не начато

**Описание:**  
Перенести функцию `format_duration` в общий модуль `src/utils` и использовать ее в GUI и converters.

**Затронутые файлы:**
- `src/gui/utils.py` (строки 8-37)
- `src/converters/base/converter.py` (строки 20-49)
- Опционально: `src/utils/` (если решите переносить в общий модуль)

**Подзадачи:**

- [ ] 6.2.1 Создать модуль `src/utils/time_utils.py`
- [ ] 6.2.2 Перенести функцию `format_duration` в новый модуль
- [ ] 6.2.3 Рефакторить `src/gui/utils.py` для использования общей функции
- [ ] 6.2.4 Рефакторить `src/converters/base/converter.py` для использования общей функции
- [ ] 6.2.5 Обновить существующие тесты
- [ ] 6.2.6 Добавить unit-тесты для нового модуля

**Предлагаемое решение:**
```python
# Создать src/utils/time_utils.py:
"""Утилиты для работы со временем."""

def format_duration(seconds: float) -> str:
    """
    Форматирование длительности в читаемый вид.
    
    Args:
        seconds: Длительность в секундах
    
    Returns:
        Отформатированная строка (например: "1ч 23м 45с")
    """
    # Единая реализация
```

**Тесты для обновления:**
- `tests/unit/test_registry_complete.py` - обновить импорты
- Все тесты конвертеров - обновить импорты

**Тесты для добавления:**
- Расширить существующие тесты конвертеров (`test_registry_complete.py` или `test_dataprocessor_converter.py`):
  - Тест форматирования секунд
  - Тест форматирования минут
  - Тест форматирования часов
  - Тест форматирования дней
  - Тест форматирования комбинированных значений
  - Тест форматирования нулевой длительности
  - Тест форматирования отрицательных значений

**Риски:**
- Минимальный риск

---

## Сводная статистика

### По приоритетам
- 🔴 Высокий: 2 задачи (1 завершена)
- 🟡 Средний: 4 задачи
- 🟢 Низкий: 2 задачи

### По сложности
- S (1-2 часа): 3 задачи
- M (3-6 часов): 3 задачи
- L (1-2 дня): 2 задачи (1 завершена)

### По модулям
- `src/converters/`: 3 задачи (1 завершена)
- `src/core/`: 2 задачи
- `src/gui/`: 3 задачи

### Общий прогресс
- ⬜ Не начато: 8 задач
- 🔄 В работе: 0 задач
- ✅ Завершено: 1 задача (11%)

---

## Рекомендуемый порядок выполнения

1. **Этап 1** (Высокая критичность, converters): Задачи 1.1, 1.2
2. **Этап 3** (Высокая критичность, core): Задача 3.1
3. **Этап 2** (Средняя критичность, converters): Задачи 2.1, 2.2
4. **Этап 4** (Средняя критичность, core): Задача 4.1
5. **Этап 5** (Средняя критичность, gui): Задача 5.1
6. **Этап 6** (Низкая критичность, gui): Задачи 6.1, 6.2

---

## Примечания

- После завершения задачи необходимо запустить полный набор тестов
- Для задач высокой критичности рекомендуется code review
- Обновление документации выполняется после завершения каждого этапа (не создавать новые md файлы)
